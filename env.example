# 环境配置示例

# .env (共用基础配置，已提交到git)
ERP_RUN_FLOW_PATH=/api/runFlow
ERP_OPEN_RUN_FLOW_PATH=/api/open/runFlow
ERP_TIMEOUT=100000
ERP_APP_VERSION=v1.1.96
PORT=9001

# .env.dev (开发环境，需要自己创建)
NODE_ENV=development
ERP_BASE_URL=https://your-dev-erp-server.com
ERP_AUTHORIZATION=Bearer your_dev_jwt_token

# .env.prod (生产环境，需要自己创建)
NODE_ENV=production  
ERP_BASE_URL=https://your-prod-erp-server.com
ERP_AUTHORIZATION=Bearer your_prod_jwt_token

# 使用方法:
# 1. 基础配置在 .env 文件中（已提交）
# 2. 创建 .env.dev 和 .env.prod 填入敏感信息
# 3. 运行: pnpm dev 或 pnpm prod
